[project]
name = "day-one"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiosqlite>=0.22.1",
    "discord-py>=2.7.1",
    "python-dotenv>=1.2.2",
]

[project.scripts]
day-one = "bot:main"

[tool.uv]
package = true